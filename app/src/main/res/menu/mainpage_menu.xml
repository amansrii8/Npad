<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <item
        android:id="@+id/action_delete"
        android:icon="@drawable/ic_action_name"
        android:title="@string/menu_title_delete_note"
        app:showAsAction="always"
        android:visible="false" />

    <item
        android:id="@+id/action_pin"
        android:icon="@drawable/ic_action_pin"
        android:title="@string/pin_the_note"
        app:showAsAction="ifRoom"
        android:visible="false" />
    <item
        android:id="@+id/action_unpin"
        android:icon="@drawable/ic_action_unpin"
        android:title="@string/unpin_the_note"
        app:showAsAction="ifRoom"
        android:visible="false" />


</menu>